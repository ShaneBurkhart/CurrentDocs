<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=1024">
    <title><%= @plan.filename %></title>
    <%= favicon_link_tag %>

    <style type="text/css">
    html{
      height: 100%;
    }
    </style>
  </head>
  <body style="height: 100%;margin:0">
    <% if not @missing_url %>
      <% if @plan.has_attribute? "plan_id" %>
        <embed src="<%= @plan.plan_record.url %>" width="100%" height="100%" style="height: 100%;">
      <% else %>
        <embed src="<%= @plan.plan.url %>" width="100%" height="100%" style="height: 100%;">
      <% end %>
    <% else %>
      <div>There doesn't seem to be an attachment here.</div>
     
    <% end %>
  </body>
</html>
