<% flash.each do |name, msg| %>
  <% if msg.is_a?(String) %>
    <script>
      console.log("Test <%= name %> <%= msg %>")
      toastr['warning']("Hello");
      toastr['<%= name == :notice ? "success" : "error" %>']('<%= msg %>')
    </script>
  <% end %>
<% end %>
